---
description: Backend 修改现有代码时保留类型/import，改完后跑 ruff + 测试验证
globs:
  - backend/**/*.py
  - mcp-lark/**/*.py
alwaysApply: false
---

# Backend 修改现有代码

修改 `backend/` 或 `mcp-lark/` 里**已有**代码时，遵守以下约束。Ruff 细则见 [`.agents/skills/backend-ruff/SKILL.md`](.agents/skills/backend-ruff/SKILL.md)。

## 保留类型与 import

- **不要随意删除** `typing`、`collections.abc`、`pydantic` 等模块中的类型注解、类型别名、`TypeVar`、`Protocol`，以及文件顶部的 import。
- 重构时若某 import「看起来未使用」，先确认是否仍被注解、`TYPE_CHECKING` 块、泛型参数或字符串前向引用使用；**不要**为「清理 unused import」而删掉仍参与类型契约的定义。
- 调整函数签名时，优先**收窄或扩展**类型，而不是移除类型信息；若必须改类型，同步更新所有调用方与相关测试。

```python
# ❌ BAD — 删掉 Annotated / 返回类型，调用方与 IDE 失去约束
async def get_user(id: str):
    ...

# ✅ GOOD — 保留或收紧类型
async def get_user(id: str) -> User | None:
    ...
```

## 改完后必须验证

完成修改后**不要**仅凭静态阅读宣布完成。每次改动 `backend/` 或 `mcp-lark/` 的 Python 代码，**必须先跑 Ruff**（与 CI / README 一致），再跑测试。

### 1. Ruff（必跑，两条命令都要执行）

在仓库根目录执行：

```bash
ruff check backend mcp-lark
ruff format backend mcp-lark
```

- 两条命令都须 **exit 0** 才能声称完成。
- `ruff format` 会写盘；若有格式化 diff，纳入同一次改动。
- CI 使用 `ruff format --check`；本地用 `ruff format` 直接修复，避免 push 后格式检查失败。
- 可自动修复 lint 时：`ruff check backend mcp-lark --fix`，然后重跑上面两条。

### 2. 相关测试

```bash
cd backend
pytest tests/test_<相关模块>.py -m "not network" -q
```

若影响面大或改动编排/路由，跑全量默认套件：

```bash
cd backend
pytest tests/ -m "not network" -q
```

`mcp-lark/` 改动时：

```bash
cd mcp-lark && pytest tests/ -q
```

### 3. 快速 import 检查（可选）

```bash
cd backend && python -c "from app.<模块> import <符号>"
```

Ruff 或测试失败时，先修复再继续提交或声称完成。
