[pytest]
pythonpath = .
asyncio_mode = auto
addopts = -m "not network"
markers =
    network: calls external LLM/search APIs (run with pytest -m network)
    integration: full product_info pipeline with real URL, Playwright, and LLM (run with pytest -m integration)